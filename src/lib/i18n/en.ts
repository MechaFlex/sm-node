const en = {
  speakerslist: {
    title: "Speaker's list",
    currently_speaking: "Currently speaking",
    returning_speakers: "Returning speakers",
  },
}

export default en

// English will act as the source of truth for the locale string structure
export type LocaleStrings = typeof en
